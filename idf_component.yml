## Project-level registry deps (also declared under main/ + camera_bsp/ so the
## component manager always sees them before CMake resolves REQUIRES).
dependencies:
  espressif/esp32-camera:
    version: ">=2.0.9"
  espressif/esp_lcd_gc9a01:
    version: "^2.0.4"
  espressif/esp_tinyusb:
    version: "^2.1.1"
  espressif/led_strip:
    version: ">=2.5.0"
