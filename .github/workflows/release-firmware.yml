# Build firmware and publish GitHub Pages + Release assets on version tags only.
name: Release firmware and Pages

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build with ESP-IDF
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v5.5.1
          target: esp32s3
          command: idf.py build

      - name: Stage binaries for web installer
        run: |
          mkdir -p docs/firmware
          cp build/bootloader/bootloader.bin docs/firmware/
          cp build/partition_table/partition-table.bin docs/firmware/
          cp build/esp32s3_cam_retro.bin docs/firmware/
          cp build/flasher_args.json docs/firmware/

      - name: Generate manifest.json from flasher_args.json
        env:
          TAG: ${{ github.ref_name }}
        run: |
          python3 <<'PY'
          import json, os
          with open("build/flasher_args.json", encoding="utf-8") as f:
              fa = json.load(f)
          parts = []
          for offset_str, file_path in fa["flash_files"].items():
              path = file_path.replace("\\", "/")
              if path.startswith("build/"):
                  path = "firmware/" + os.path.basename(path)
              parts.append({"path": path, "offset": int(offset_str, 0)})
          parts.sort(key=lambda p: p["offset"])
          manifest = {
              "name": "ESP32-S3 CAM Retro",
              "version": os.environ.get("TAG", "v0.0.0").lstrip("v"),
              "new_install_prompt_erase": True,
              "builds": [{"chipFamily": "ESP32-S3", "parts": parts}],
          }
          with open("docs/manifest.json", "w", encoding="utf-8") as f:
              json.dump(manifest, f, indent=2)
              f.write("\n")
          print(json.dumps(manifest, indent=2))
          PY

      - name: Set manifest version from tag
        env:
          TAG: ${{ github.ref_name }}
        run: |
          python3 <<'PY'
          import json, os
          tag = os.environ.get("TAG", "v0.0.0")
          ver = tag.lstrip("v")
          with open("docs/manifest.json", encoding="utf-8") as f:
              m = json.load(f)
          m["version"] = ver
          with open("docs/manifest.json", "w", encoding="utf-8") as f:
              json.dump(m, f, indent=2)
              f.write("\n")
          print(f"manifest.json version -> {ver}")
          PY

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            docs/firmware/bootloader.bin
            docs/firmware/partition-table.bin
            docs/firmware/esp32s3_cam_retro.bin
            docs/firmware/flasher_args.json
          generate_release_notes: true

      - name: Deploy to GitHub Pages (gh-pages branch)
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
          publish_branch: gh-pages
